---
import BaseHead from "~/components/BaseHead.astro";
import "../styles/global.scss";
import "../styles/sitheme.css";

// Define props with TypeScript interface
interface Props {
  title: string;
  description?: string;
  ogImage?: string;
  canonicalURL?: string;
}

// Destructure props with default values
const {
  title = "StudentImpact - 国際的なキリスト教インカレ団体",
  description = "一生涯の友と一生涯の成長を:StudentImpactは国際的なキリスト教インカレ団体です",
  ogImage = "/images/og-image.jpg",
  canonicalURL = Astro.url.href,
} = Astro.props;
---

<!doctype html>
<html class="no-js" lang="jp">
  <head>
    <BaseHead
      title={title}
      description={description}
      ogImage={ogImage}
      canonicalURL={canonicalURL}
    />
  </head>
  <body
    data-mobile-nav-trigger-alignment="center"
    data-mobile-nav-style="classic"
    data-mobile-nav-bg-color="#1d1d1d"
  >
    <!-- Main Content Slot -->
    <slot />

    <!-- Start Scroll Progress -->
    <div class="scroll-progress d-none d-xxl-block">
      <a href="#" class="scroll-top" aria-label="scroll">
        <span class="scroll-text">Scroll</span>
        <span class="scroll-line">
          <span class="scroll-point"></span>
        </span>
      </a>
    </div>
    <!-- End Scroll Progress -->

    <!-- JavaScript Libraries -->
    <script is:inline src="/js/jquery.min.js"></script>
    <script is:inline src="/js/vendors.min.js"></script>
    <script is:inline src="/js/main.js"></script>

    <!-- Additional Scripts -->
    <slot name="additional-scripts" />
  </body>
</html>
